```dataviewjs
const tags = dv.pages().file.tags
  .distinct()
  .sort(t => dv.pages().where(p => p.file.tags.includes(t)).length, 'desc')
  .limit(10);

dv.table(["标签", "笔记数"], 
  tags.map(t => [t, dv.pages().where(p => p.file.tags.includes(t)).length])
);
```
