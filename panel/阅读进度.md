
```dataviewjs
const books = dv.pages('"阅读"').where(p => p.status);
const total = books.length;
const read = books.where(p => p.status == "已读").length;
const pct = total ? Math.round(read/total*100) : 0;

const bar = "▰".repeat(Math.floor(pct/10)) + "▱".repeat(10-Math.floor(pct/10));
dv.paragraph(`📚 已读 ${read}/${total}（${pct}%）`);
dv.paragraph(`\`${bar}\``);
```
