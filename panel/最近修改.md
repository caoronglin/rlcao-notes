```dataviewjs
const pages = dv.pages()
  .where(p => !p.file.path.contains("仪表盘") && p.file.path != dv.current().file.path)
  .sort(p => p.file.mtime, 'desc')
  .limit(8);
dv.table(["笔记", "修改时间"], 
  pages.map(p => [p.file.link, moment(p.file.mtime).format("MM-DD HH:mm")])
);
```